stages:
  - validate

variables:
  DOCKER_TLS_CERTDIR: "/certs"

platform-validate:
  stage: validate
  image: docker:27-cli
  services:
    - docker:27-dind
  before_script:
    - apk add --no-cache bash git py3-yamllint
  script:
    - ./scripts/validate.sh
